BITCOIN_CREDENTIAL_FILE=/absolute/path/to/restricted-rpc-cookie
BITCOIN_DISK_PROBE_PATH=/absolute/path/on/bitcoin-data-filesystem
# Plain HTTP is accepted only on loopback. For a remote/container connection,
# use HTTPS or terminate an SSH tunnel on loopback.
BITCOIN_RPC_URL=http://127.0.0.1:8332
